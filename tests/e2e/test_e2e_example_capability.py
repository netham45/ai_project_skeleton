def test_example_capability_placeholder() -> None:
    raise AssertionError(
        "Template example only. Replace tests/e2e/test_e2e_example_capability.py with a real E2E narrative "
        "before claiming runtime proof for a concrete feature."
    )
