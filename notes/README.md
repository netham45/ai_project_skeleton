# Notes

This folder holds the design, governance, inventory, and traceability artifacts that keep implementation aligned with declared intent.

The starter repo treats these notes as implementation assets, not optional commentary.

This note tree is also where the cloned repository should declare its real stack choices during genesis and architecture instead of inheriting them implicitly from the starter.
