# Feature Checklist: PF01 Example Capability

## Purpose

Example per-feature checklist showing how a decomposed user-requested capability should be tracked once product features exist.

Replace this file with a real feature checklist when the original project vision is known.

## Traceability

- Feature ID: `PF01`
- Source vision reference: `notes/explorations/original_concept.md#capability-1`
- Governing flow IDs: `FLOW10`
- Rigid feature definition file: `plan/features/PF01_example_capability.yaml`
- Feature snapshot baseline: `notes/catalogs/verification/PF01_snapshot.json`
- Documentation surfaces:
  - `docs/user/example.md`
  - `docs/operator/example.md`
- Bounded proof command: `python3 -m pytest tests/unit/test_product_feature_traceability_docs.py -q`
- E2E asset: `tests/e2e/test_e2e_example_capability.py`
- Real E2E command: `python3 -m pytest tests/e2e/test_e2e_example_capability.py -q`
- Last snapshot update command: `python3 scripts/update_feature_snapshot.py --feature PF01`
- Last snapshot result: `not_run`
- Last E2E command run: `not_run`
- Last E2E result: `not_run`

## Status

| Area | Status | Notes |
| --- | --- | --- |
| Database | planned | Replace with the real affected-system posture once the product feature is known. |
| CLI | planned | Replace with the real affected-system posture once the product feature is known. |
| Daemon or backend | planned | Replace with the real affected-system posture once the product feature is known. |
| Website UI | planned | Replace with the real affected-system posture once the product feature is known. |
| Config or YAML | planned | Replace with the real affected-system posture once the product feature is known. |
| Prompts | planned | Replace with the real affected-system posture once the product feature is known. |
| User documentation | planned | Replace with the real affected-system posture once the product feature is known. |
| Notes | planned | Replace with the real affected-system posture once the product feature is known. |
| Bounded tests | planned | Replace with the real bounded proof once the product feature is known. |
| E2E | planned | Replace with the real E2E proof once the product feature is known. |
| Snapshot | planned | Replace with the real snapshot posture once the product feature is known. |
| Overall | planned | Placeholder checklist only. |

## Known Limitations

- This is a template example, not a real product feature claim.
- Replace this file when the original project vision is decomposed into real product features.
